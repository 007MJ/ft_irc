**********************************************************************************************************
1 - Créer un serveur (socket, bind, listen, accept, etc) close
    * PORT
    * Liste des Clients
    * Créer class CommandHandler() KICK, INVITE, TOPIC, MODE

2 - Créer class Client (nickname, fd)
    * Méthode send() --- Possibilité de send un message à un autre client en privé

3 - Créer class Channel
    * Liste de users (connaître les super-users)
